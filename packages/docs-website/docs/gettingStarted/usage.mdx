---
sidebar_position: 2
---

# Usage

The following example of code to use simple `Box` component using beyond-ui.

```jsx
import React from 'react';
import { BuiBox } from '@beyondui/react';

export const Container = ({ children }) => {
    return(
        <BuiBox>{children}</BuiBox>
    )
};
```